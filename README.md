# One Tailor Application

A premium web application for tailor management built with React and Node.js.

## Project Structure
- `backend/`: Node.js Express server.
- `frontend/`: React Vite application.

## How to Run

### 1. Start the Backend
Open a terminal and run:
```bash
cd backend
npm run dev
```
The server will start on `http://localhost:5000`.

### 2. Start the Frontend
Open another terminal and run:
```bash
cd frontend
npm run dev
```
The application will be available at the URL shown in the terminal (usually `http://localhost:5173`).

## Login Credentials
- **Username**: admin
- **Password**: admin

## Features
- Premium Dark Glassmorphism UI.
- Searchable customer table.
- Add, Edit, and Delete customers.
- Comprehensive customer data fields (Sizes, Mobile, Price, Address, etc.).
- Unique User ID tracking.
